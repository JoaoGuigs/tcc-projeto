{
  "name": "tcc-projeto",
  "version": "1.0.0",
  "description": "Sistema de gestao para clinica de fisioterapia",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "verify": "npm run test --workspace server && npm run test --workspace client && npm run typecheck --workspace client && npm run lint --workspace client && npm run build --workspace client"
  },
  "license": "ISC",
  "engines": {
    "node": ">=24 <25"
  }
}
